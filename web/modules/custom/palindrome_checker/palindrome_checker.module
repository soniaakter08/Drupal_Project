<?php

/**
 * Implements hook_help().
 */
function palindrome_checker_help($route_name, $route_match) {
  switch ($route_name) {
    case 'help.page.palindrome_checker':
      return '<p>This module allows you to check if a word or phrase is a palindrome.</p>';
  }
}
